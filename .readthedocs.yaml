version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install poetry
    - poetry config virtualenvs.create false
    - poetry install --with docs
    - python -m sphinx -T -b html -d $READTHEDOCS_OUTPUT/doctrees docs/source $READTHEDOCS_OUTPUT/html
